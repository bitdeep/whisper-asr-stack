services:
  speaches:
    image: ${WHISPER_IMAGE:-whisper-asr-stack:0.1.0}
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 8g
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
