services:
  speaches:
    extends:
      file: engine.compose.yaml
      service: speaches
    build: .
    ports:
      - "127.0.0.1:${HTTP_PORT:-8010}:8000"
    volumes:
      - models:/home/ubuntu/.cache/huggingface

volumes:
  models:
